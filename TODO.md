# TODO

进度快照与待办。最后更新：2026-07-18

---

## 已完成（当前状态）

工具本体
- [x] 剪贴板 → pandoc + typst → 后台静默投递管道（`deliver.sh`）
- [x] 按 QUADERNO A5 显示区物理尺寸出页，实现 1:1 零缩放（**已用钢尺实测确认**）
- [x] 失败反馈：空剪贴板 / 渲染失败 / 设备离线 / 未确认送达，四类通知
- [x] `--check` 全链路自检
- [x] 已修：locale 乱码、PATH 缺 Homebrew、`@` 引发渲染崩溃、`open -na` 抢焦点
- [x] 投递提速：轮询 1s → 0.15s，日志路径缓存
- [x] 全局热键托管（macOS 快捷指令，⌃⌥⌘P）

测定与文档
- [x] 显示区物理尺寸推导法 + 双重自验证
- [x] `calibrate.typ` 1:1 校准页、`font-menu.typ` 字号菜单
- [x] 技术文档 `docs/quaderno-display-metrics.md`（含钢尺实测照片）
- [x] 官网信息架构草案、单页文案稿（中文）
- [x] MIT LICENSE、英文 README、git 身份改写

---

## 阻塞中 —— 等待决定或外部操作

- [x] ~~确认 GitHub 账号名~~ —— 已验证 `leekaomin380`
- [x] ~~创建 GitHub 公开仓库并推送~~ —— 2026-07-26 完成，
      **https://github.com/leekaomin380/eink-truescale-pdf**（公开，MIT）
- [ ] **网站集成方案**
      已交由独立 session 评估现有官网，等它反馈技术栈与 i18n 成本
- [ ] **英文版网页文案**
      刻意延后：等网站 i18n 成本明确后再写，避免白写

---

## 已完成（2026-07-30）

- [x] **排版偏好持久化** —— 字体/字号/边距/行距/目标设备不再每次启动打回默认
      关键取舍：**不信任存下来的值**。字体会被卸载、选项表会变，若读到什么就用什么，
      Picker 会选中不存在的项而显示空白；更糟的是字体失效时 typst 会**静默 fallback**，
      排版被悄悄换掉而用户毫无察觉。故取用前逐项校验，字体由 `reconcileSavedFonts`
      在异步字体列表就绪后事后核对。
- [x] **`.app` 自带 pandoc / typst** —— 无 Homebrew 的机器上装上即可用（306MB，仅 arm64）
      顺带消掉 poppler 整棵依赖树：它仅被 `pdfinfo` 用于读页面尺寸，改用 PDFKit 读
      mediaBox 后不再需要，也免去正则解析（此前漏取捕获组致「尺寸不一致」恒亮告警）。
      许可义务见 `THIRD-PARTY-LICENSES.md`。
- [x] **数学公式**：恢复公式支持（此前误关 `tex_math_dollars` 致公式内中文与希腊字母
      被整段丢弃）；超宽公式等比缩入版心（此前左右各溢出约 10mm）
- [x] 发送/另存自行保证内容最新（此前不点预览直接发送会发出**上一篇**）
- [x] **网页链接支持任意站点**（原仅限微信公众号，用户真机确认可用）
      抽取器本就大半通用（标题/作者/日期取标准 `og:` 与 `<title>`/`<h1>`），
      只有正文容器选择器是公众号专用。新增按语义精确度降序的 fallback
      （`article` → `[role=main]` → `main` → `.post-content` …），并剥离
      `NAV`/`HEADER`/`FOOTER`/`ASIDE`。**公众号选择器必须最先匹配**——那条路
      针对单一站点调过（反爬、`data-src` 懒加载、135editor 内嵌 style）。
      顺带修掉「相对图片地址被 ImageInliner 整条跳过 → 图片静默消失」：
      实测一篇 36 张图的文章抽取后一张都没进 PDF，且无任何报错。
      **局限**：重 JS 渲染 / 付费墙 / 强反爬的站点必然失败（WKWebView 全量断网），
      定位是尽力而为 + 失败时如实告知，失败已分类为 `NEEDS_JS` / `STRUCT_MISSING`。
- [x] **`.app` 整包签名**（此前从未签 bundle 本体，下载后 macOS 报「已损坏」）
      仅解决「已损坏」；真正零摩擦仍需 Developer ID + 公证，见「搁置」。
- [x] **`book.sh -o` 相对路径产物被临时目录吃掉**（脚本却报成功，静默数据丢失）
- [x] **只改排版参数时预览不重画**（磁盘 PDF 已更新，只有预览图骗人）

---

## 已决定不做

- [x] ~~**把全局热键内建进 app**~~ —— **2026-07-30 决定不做**
      技术上可行且已验证（Carbon `RegisterEventHotKey` 无需辅助功能权限即可注册，
      实测 `AXIsProcessTrusted()` 为 false 时仍注册成功；agy 的 T-007 实现失败是
      因调用顺序问题，非平台限制）。

      **不做的理由**：需要 app 常驻菜单栏、可能还要开机自启，是形态层面的改动；
      而收益存疑 —— 提出需求的人自己「几乎已经不用热键了」，GUI 已覆盖同样场景。
      为一个使用者自己都放弃的入口改变 app 形态，代价高于收益。

      需要热键的用户仍可用 Shortcuts.app 绑定（README 有一分钟的配置步骤）。
      任务书 `tasks/T-007.md` 保留作记录，不再执行。

---

## 分发准备

- [ ] A4 版校准页 PDF（网页上已有下载按钮，文件尚未生成）
      改 `calibrate.typ` 顶部 `PW`/`PH` 为 202.69mm / 270.26mm 后重新编译
- [ ] 通知与错误信息英文化（当前为中文，与英文 README 不一致）
- [ ] 安装脚本（可选；已明确**不做** `curl | sh`，理由见 README）
- [ ] Homebrew tap（远期，需先有 GitHub 仓库）
- [ ] **把 `.app` 作为 Release 资产上传**（306MB，仅 arm64）
      自包含已完成，这是让「下载即用」真正成立的最后一步；
      需先确认是否愿意公开分发含 GPL 组件的二进制（义务已在
      `THIRD-PARTY-LICENSES.md` 中履行）

---

## 工具增强

- [ ] **乐观通知** —— `open` 后立即弹 ✅（约 0.6s），验证转入后台，失败再补 ❌
      感知延迟可砍半。代价：✅ 含义从「已确认送达」变为「已交客户端」
- [ ] **`--render-only` 模式** —— 只渲染不投递
      一举三得：脱离 QUADERNO 独立使用 / 留副本（客户端会删源文件）/ 调版面时不污染设备
- [ ] **智能命名 + PDF 元数据**
      现为 `quaderno_delivery_<PID>.pdf`，投递后无法检索。方向：
      抓首个 H1 作标题 → 加时间戳前缀 → **写入 PDF metadata title/author**
      （客户端文件列表有「标题」「作者」两列，很可能读的就是元数据，
       写入后可在客户端内搜索，检索力强于文件名）
- [ ] **A4 机型支持** —— 参数已备好，但 202.69 × 270.26 mm 仍是推导值，需实测

---

## 新功能：电子书 → Quaderno（epub / mobi / azw）

**动机**：QUADERNO 只接受 PDF，而多数电子书是 epub/mobi/azw。
过去转换「效果很不好」——根因大概率就是页面尺寸错配导致二次缩放 + 按纸张习惯留的大边距。
现在我们有了确切的显示区尺寸，可以从根上解决。

已确认的技术事实（本次实测）：
- ✅ pandoc **原生支持 epub 输入**（`epub` / `fb2` / `html` 均在支持列表）
- ✅ typst **自动从标题层级生成 PDF 大纲**（`/Outlines`），无需额外开发
- ❌ mobi/azw **需要 calibre 的 `ebook-convert`**（当前未安装），
      路径为 mobi/azw3 → epub → 本管道

待办：
- [x] **QUADERNO 阅读器支持 PDF 大纲且能跳转**（已实测确认，导航零额外开发）
      PDF 侧已确认能生成书签，但设备端是否暴露该功能只能实测。
      若支持 → 长书可直接跳章，体验接近原生电子书
      若不支持 → 需改为在正文中生成可点击的目录页（`--toc`）
- [x] 章节分页（每章另起一页）
- [x] 插图: 真书含 17 张图，正常带入
- [ ] 脚注、注释的呈现
- [x] 长文档实测: 3.2MB epub → 260 页 6MB PDF，耗时仅 2 秒（远优于预期）
- [ ] 客户端上传上限仍未测（6MB 已可投递，更大的书待验）
- [x] 元数据继承：书名/作者已随 epub 带入
- [x] mobi/azw 支持路径已实现（依赖 calibre；**DRM 文件不在支持范围内**）

---

## 搜索可见性（待优化，已做初步调研）

**目标**：让中英文用户搜「Quaderno」「电子墨水 公众号」等词时能找到本项目。

### 2026-07-26 调研发现（实测 `gh search`）

1. **本仓库搜 `quaderno` 前 50 名内不可见。**
   直接原因：**GitHub 站内搜索极度依赖仓库名**，而 `eink-truescale` 不含该词。
2. **「Quaderno」是撞名词。** 搜索前排几乎被同名的发票/税务 SaaS
   （`quaderno/*` 组织，10+ 个仓库）占据，与电子纸设备无关。
   真正的同类项目排在其后：`ikanher/quaderno-gui`(9⭐)、
   `ygjsz/A4_fw_unpacker`(23⭐)、`cristobaltapia/dpt-rp1-cups`(9⭐)。
3. 上述同类项目**均未设置 topics** —— 它们靠仓库名里的关键词被搜到。
4. 中文词（如「公众号 epub」）在 GitHub 站内基本搜不出结果，
   **中文用户的入口更可能是 Google / 微信搜一搜 / 小红书，而非 GitHub 站内**。

### 2026-07-30 复测与决定

重跑搜索，得到更细的排名（`gh api search/repositories`，取前 100 名）：

| 搜索词 | 排名 |
|---|---|
| `quaderno` | ❌ 前 100 名外 |
| `epub to pdf e-ink` | ❌ 前 100 名外 |
| `微信公众号 pdf` | 第 46 名（第 3 页，等于看不见） |
| `eink pdf epub` | ✅ 第 6 名 |
| `e-ink true size pdf` | ✅ 第 1 名 |
| `topic:quaderno` | 第 7 / 共 7（最后一名） |

**关键发现：GitHub 默认搜索只覆盖仓库名 + 描述，不覆盖 README。**
而我们的描述里当时**根本没有 QUADERNO 这个词** —— 这才是搜 `quaderno`
不可见的直接原因，不只是仓库名的问题。

**决定：改名为 `eink-truescale-pdf`，并重写描述。**

- 名字**不含** `quaderno`，守住 PRD §8.1-1「中性命名，不以其商标为产品名」
- 商标以**事实性兼容声明**写进描述（`Works with Fujitsu QUADERNO A5/A4`），
  这是 §8.1-2 明确允许的名义性合理使用，且描述可被搜索 —— 两个约束同时满足
- 补上 `pdf` 一词（实测 `eink pdf epub` 能进第 6，说明该词有效，而原名不含）
- 保留 `eink-truescale` 前缀，不浪费既有品牌与外链

**须承认的局限**：改名让本项目「能被搜到」，不等于「排前面」。
`quaderno` 前排是同名发票 SaaS（7–15⭐），本仓库 0⭐ 很难挤入。
真正的流量仍取决于下面一行的官网 SEO。

### 可选做法与权衡

| 做法 | 收益 | 代价 |
|---|---|---|
| ~~仓库改名含 `quaderno`~~ | —— | **已否决**：违反 §8.1-1。改用「中性名 + 描述中作兼容声明」达到同等效果。另：此前所写「改名会断掉现有链接」**不准确** —— GitHub 会保留旧地址重定向 |
| README 增加关键词自然出现频次 | 无副作用 | 效果有限，站内搜索仍偏重仓库名 |
| 官网技术文章做 SEO | 面向 Google，覆盖中英文，且能承载长尾词（「电子墨水 PDF 模糊」等） | 依赖官网集成落地，见「阻塞中」 |
| 在同类项目 issue / 相关社区留言 | 精准触达真实用户 | 需人工、注意不要变成刷广告 |

**当前倾向**：不改仓库名（定位优先于短期搜索排名），
主要靠官网 SEO + 长尾词承接，GitHub 站内作为次要入口。
**此判断可随时推翻**，若发现实际用户确实主要从 GitHub 站内发现工具。

---

## 数据征集与社区

- [ ] **QUADERNO A4 实测数据**（目前唯一的推导值，最优先）
- [ ] 其他厂商设备：reMarkable / BOOX / Kindle Scribe
- [ ] 设备参数库页面 + `devices.json` 机器可读数据
      **注意**：目前仅 1 条实测数据，不要为「数据库」过度设计，
      先让单篇文章站住，有第二、三条数据再谈库
- [ ] 官网 `/tools` 工具子板块（已明确延后）

---

## GUI（已完成原生重写）

- [x] 本地 Web 界面 `gui.sh` —— EPUB → PDF，A5 目标（历史版本，见下方说明）
- [x] 拖拽载入、字体/字号/边距/行距/语言可调
- [x] 实时数字读数（版心、中英行长 + 合规判定），参数改动即时反馈
- [x] 等比预览（任意页），标注「字号舒适度以设备为准」
- [x] 一键发送到设备（投递副本，保护产物）/ 另存 PDF
- [x] 自动校验不变量 I1（全书页面尺寸是否统一）
- [x] **包成 .app** —— 已从「WKWebView + Python 服务」重写为**纯原生 SwiftUI**
      （`QuadernoApp.swift`/`ContentView.swift`/`ConversionViewModel.swift`，
      PDFKit 直接渲染预览，无网络请求、无 HTTP 服务）
- [x] **粘贴文本模式**（Markdown 直接转 1:1 PDF，`book.sh --plain`）
- [ ] 「实际尺寸」预览模式（需一次性尺子校准显示器 PPI）
- [ ] 转换进度反馈（目前长书期间界面静默）

**关于 `gui.sh`（Python + HTML 版本）**：功能仍完整（含粘贴文本模式），
与原生 .app 并行存在。是否保留作为不需要 Xcode 的轻量备选，待定。

---

## 备忘：不要重蹈的坑

来自开发期实战，详见 `docs/development-log-zh.md`：

- 脚本必须强制 `LANG`/`LC_ALL`，否则 pandoc 把 UTF-8 中文当 latin1 → 乱码
- 脚本必须自己补 `PATH`，快捷指令给的 PATH 不含 Homebrew
- Markdown 方言必须关 `citations` 与 `tex_math_dollars`
  （`@227dpi` 会被当文献引用致崩溃；`$PATH` 会被当数学公式吃掉）
- `open` 的 `-n` 不可省，macOS 不向已运行实例传 `--args`
- 渲染必须在 `/tmp` 沙盒，否则 pandoc 在只读根目录建临时目录时崩溃
- 客户端上传后会 `unlink` 源 PDF；成功无日志，只有失败才写 log
